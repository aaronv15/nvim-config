return {
   'OXY2DEV/markview.nvim',
   lazy = false,

   opts = {
      -- markdown = { enable = false },
      -- markdown_inline = { enable = false },
   },
   -- If error shows up, try enabling this?
   -- Completion for `blink.cmp`
   -- dependencies = { 'saghen/blink.cmp' },
}
